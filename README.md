Taiga Stream (iOS)

This application allows you configure up to eight streaming audio URLs, and then play each stream using a button from the control center or lock screen. These buttons function like a configurable version of the default iOS ambient music buttons. The interface is intentionally minimalist, with no additional media controls or content. If your device is connected to an iCloud account, your URL list will sync across all devices on your account.
